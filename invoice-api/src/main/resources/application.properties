spring.application.name=invoice-api

# ===============================
# PostgreSQL Configuration
# FIX #5: All connection values are externalised via env vars so the same
# image works both locally (localhost) and inside Docker (service names).
# ===============================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/sap_invoices}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===============================
# RabbitMQ Configuration
# ===============================
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${SPRING_RABBITMQ_PORT:5672}
spring.rabbitmq.username=${SPRING_RABBITMQ_USERNAME:admin}
spring.rabbitmq.password=${SPRING_RABBITMQ_PASSWORD:password}

# ===============================
# Azurite (Local Azure Storage)
# FIX: azure.storage.connection-string is what Spring Boot's OWN client uses
# to reach Azurite (container creation, SAS generation) — this must be the
# real network address (Docker service name "azurite" when containerized,
# "127.0.0.1" when running outside Docker). It is NEVER browser-facing.
#
# azure.storage.public-host is ONLY used to rewrite the hostname in the
# generated SAS URL before it's returned to the browser, since the browser
# runs outside the Docker network and can only reach Azurite via the
# published port on 127.0.0.1. In production against real Azure Storage,
# leave this unset — the SDK-provided host is already public.
# ===============================
azure.storage.connection-string=${AZURE_STORAGE_CONNECTION_STRING:DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlqUwJPLlmEtlCDXJ1OUzFT50uSRZ6IFsuFq2UVErCz4I6tq/K1SZFPTOtr/KBHBeksoGMGw==;BlobEndpoint=http://127.0.0.1:10000/devstoreaccount1;}
azure.storage.public-host=${AZURE_STORAGE_PUBLIC_HOST:127.0.0.1}